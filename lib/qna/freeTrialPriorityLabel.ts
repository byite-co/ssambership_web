/**
 * 무료 체험(무료 질문권) 스레드의 답변 우선순위 라벨.
 * - 멘토 질문 목록에서 무료 체험 스레드를 최상단 고정 노출할 때 배지로 표시한다.
 * - 서버 전용 의존성이 없어 클라이언트 컴포넌트에서도 import 가능.
 */
export const FREE_TRIAL_PRIORITY_LABEL = "무료 체험 · 우선 답변" as const;
